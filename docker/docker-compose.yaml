version: '3.7'
services:
  mcabank-db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_ROOT_USERNAME: root
      MYSQL_DATABASE: mcabank-db
    ports:
      - "3306:3306"
    volumes:
      - .database:/var/lib/mysql
